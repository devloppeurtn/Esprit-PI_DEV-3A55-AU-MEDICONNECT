{% extends 'base.html.twig' %}

{% block title %}Événements - MediConnect{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h1 class="mb-2">Événements à venir</h1>
            <p class="text-muted">Découvrez les prochains événements et inscrivez-vous</p>
        </div>
        <div>
            {% if is_granted('ROLE_ORGANISATEUR') %}
                <a href="{{ path('app_evenement_new') }}" class="btn btn-primary">
                    <i class="bi bi-plus-circle me-1"></i>Créer un événement
                </a>
            {% endif %}
        </div>
    </div>

    <div id="alerts"></div>

<<<<<<< HEAD
    {% if aiRecommendations is defined and aiRecommendations|length > 0 %}
        <div class="alert alert-info border-0 shadow-sm mb-4" style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); color: white;">
            <div class="d-flex align-items-center mb-3">
                <i class="bi bi-robot" style="font-size: 2rem; margin-right: 15px;"></i>
                <div>
                    <h5 class="mb-1" style="color: white;">
                        🤖 Recommandations IA Personnalisées
                        {% if aiRecommendations[0].ai_generated is defined and aiRecommendations[0].ai_generated %}
                            <span class="badge bg-success ms-2" style="font-size: 0.7rem;">Propulsé par Groq Llama</span>
                        {% else %}
                            <span class="badge bg-warning text-dark ms-2" style="font-size: 0.7rem;">Mode Basique</span>
                        {% endif %}
                    </h5>
                    <small style="opacity: 0.9;">Basées sur votre dossier médical</small>
                </div>
            </div>
            
            <div class="row g-3">
                {% for recommendation in aiRecommendations %}
                    <div class="col-md-4">
                        <div class="card h-100" style="border: 2px solid rgba(255,255,255,0.3); background: rgba(255,255,255,0.1); backdrop-filter: blur(10px);">
                            <div class="card-body text-white">
                                <div class="d-flex justify-content-between align-items-start mb-2">
                                    <h6 class="card-title mb-0" style="color: white;">{{ recommendation.event.title }}</h6>
                                    <span class="badge bg-warning text-dark">Score: {{ recommendation.score }}</span>
                                </div>
                                
                                {% if recommendation.event.eventDate %}
                                    <p class="mb-2" style="font-size: 0.9rem; opacity: 0.9;">
                                        <i class="bi bi-calendar-event me-1"></i>
                                        {{ recommendation.event.eventDate|date('d/m/Y') }}
                                    </p>
                                {% endif %}
                                
                                <div class="mb-3" style="font-size: 0.85rem; opacity: 0.8;">
                                    {% for reason in recommendation.reasons %}
                                        <div class="mb-1">
                                            <i class="bi bi-check-circle me-1"></i>{{ reason }}
                                        </div>
                                    {% endfor %}
                                </div>
                                
                                <div class="d-grid gap-2">
                                    <a href="{{ path('app_evenement_show', {id: recommendation.event.id}) }}" class="btn btn-light btn-sm">
                                        <i class="bi bi-eye me-1"></i>Voir les détails
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>
                {% endfor %}
            </div>
            
            <div class="mt-3 text-center" style="font-size: 0.85rem; opacity: 0.8;">
                <i class="bi bi-info-circle me-1"></i>
                Ces recommandations sont générées automatiquement en analysant votre profil médical
            </div>
        </div>
    {% endif %}

=======
<<<<<<< HEAD
=======
>>>>>>> 4f714e473f4c6306d8cd13fadaae1828cd26d7f1
    <div class="card mb-4">
        <div class="card-body">
            <form method="get" class="row g-3 align-items-end">
                <div class="col-md-6">
                    <label class="form-label mb-1">Recherche</label>
                    <input type="text" name="q" class="form-control" placeholder="Nom ou description de l'événement..."
                           value="{{ search|default('') }}">
                </div>
                <div class="col-md-4">
                    <label class="form-label mb-1">Période</label>
                    <select name="periode" class="form-select">
                        <option value="tous" {% if periode|default('tous') == 'tous' %}selected{% endif %}>Tous</option>
                        <option value="avenir" {% if periode|default('tous') == 'avenir' %}selected{% endif %}>À venir</option>
                        <option value="passes" {% if periode|default('tous') == 'passes' %}selected{% endif %}>Passés</option>
                    </select>
                </div>
                <div class="col-md-2 d-flex gap-2">
                    <button class="btn btn-primary w-100" type="submit">
                        <i class="bi bi-search me-1"></i>Filtrer
                    </button>
                </div>
            </form>
        </div>
    </div>

    {% if mesEvenements is defined and mesEvenements|length > 0 %}
        <div class="mb-5">
            <h4 class="mb-3"><i class="bi bi-calendar-check me-2"></i>Mes événements</h4>
            <div class="row g-3">
                {% for item in mesEvenements %}
                    <div class="col-12">
                        <div class="card shadow-sm border-start border-4 {{ item.statut.value == 'VALIDE' ? 'border-success' : (item.statut.value == 'EN_ATTENTE' ? 'border-warning' : 'border-danger') }}">
                            <div class="card-body">
                                <div class="d-flex justify-content-between align-items-start flex-wrap gap-2">
                                    <div>
                                        <h5 class="card-title mb-1">{{ item.title }}</h5>
                                        <span class="badge {{ item.statut.value == 'VALIDE' ? 'bg-success' : (item.statut.value == 'EN_ATTENTE' ? 'bg-warning text-dark' : 'bg-danger') }}">
                                            {% if item.statut.value == 'EN_ATTENTE' %}En attente de validation{% elseif item.statut.value == 'VALIDE' %}Publié{% else %}Refusé{% endif %}
                                        </span>
                                        {% if item.eventDate %}<span class="badge bg-info ms-1">{{ item.eventDate|date('d/m/Y') }}</span>{% endif %}
                                    </div>
                                    <div class="btn-group btn-group-sm">
                                        <a href="{{ path('app_evenement_show', {id: item.id}) }}" class="btn btn-outline-primary"><i class="bi bi-eye"></i></a>
                                        <a href="{{ path('app_evenement_edit', {id: item.id}) }}" class="btn btn-outline-secondary"><i class="bi bi-pencil"></i></a>
<<<<<<< HEAD
                                        <form method="POST" action="{{ path('app_evenement_delete', {id: item.id}) }}" class="d-inline" onsubmit="return confirm('Supprimer cet �v�nement ?');">
=======
                                        <form method="POST" action="{{ path('app_evenement_delete', {id: item.id}) }}" class="d-inline" onsubmit="return confirm('Supprimer cet �v�nement ?');">
>>>>>>> 4f714e473f4c6306d8cd13fadaae1828cd26d7f1
                                            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ item.id) }}">
                                            <button type="submit" class="btn btn-outline-danger btn-sm"><i class="bi bi-trash"></i></button>
                                        </form>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                {% endfor %}
            </div>
        </div>
        <hr class="my-4">
        <h4 class="mb-3">Événements publiés</h4>
    {% endif %}

<<<<<<< HEAD
=======
>>>>>>> isramedi
>>>>>>> 4f714e473f4c6306d8cd13fadaae1828cd26d7f1
    <div id="items-list">
        {% for item in items %}
            <div class="card mb-4 shadow-sm">
                <div class="card-body">
                    <div class="row">
                        <div class="col-md-8">
<<<<<<< HEAD
                            <div class="d-flex align-items-center gap-3 mb-3">
                                <h5 class="card-title mb-0">{{ item.title }}</h5>
                                
                                {% if weatherData[item.id] is defined %}
                                    <div class="weather-info d-flex align-items-center gap-2 ms-2" style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); padding: 8px 16px; border-radius: 20px; color: white; box-shadow: 0 2px 8px rgba(102, 126, 234, 0.3);">
                                        <img src="https://openweathermap.org/img/wn/{{ weatherData[item.id].icon }}@2x.png" 
                                             alt="{{ weatherData[item.id].description }}" 
                                             style="width: 40px; height: 40px; filter: drop-shadow(0 2px 4px rgba(0,0,0,0.2));">
                                        <div class="d-flex flex-column" style="line-height: 1.2;">
                                            <span style="font-size: 1.2rem; font-weight: 600;">{{ weatherData[item.id].temp }}°C</span>
                                            <span style="font-size: 0.75rem; opacity: 0.9;">{{ weatherData[item.id].description }}</span>
                                        </div>
                                    </div>
                                {% endif %}
                            </div>
=======
                            <h5 class="card-title mb-3">{{ item.title }}</h5>
>>>>>>> 4f714e473f4c6306d8cd13fadaae1828cd26d7f1
                            
                            {% if item.eventDate %}
                                <div class="mb-3">
                                    <span class="badge bg-info" style="font-size: 0.95rem;">
                                        <i class="bi bi-calendar-event me-1"></i>
                                        {{ item.eventDate|date('d-m-Y') }}
                                    </span>
                                </div>
                            {% endif %}

                            {% if item.content %}
                                <p class="card-text text-muted">{{ (item.content|length > 150) ? item.content|slice(0, 150) ~ '...' : item.content }}</p>
                            {% endif %}

                            <small class="text-muted">
                                <i class="bi bi-clock"></i> Créé le {{ item.createdAt|date('d/m/Y à H:i') }}
                            </small>
                        </div>

                        <div class="col-md-4 d-flex flex-column justify-content-center">
<<<<<<< HEAD
                            <a href="{{ path('app_evenement_participer_form', {id: item.id}) }}" class="btn btn-success btn-lg mb-2">
=======
<<<<<<< HEAD
                            <a href="{{ path('app_evenement_show', {id: item.id}) }}" class="btn btn-success btn-lg mb-2">
=======
                            <a href="{{ path('app_evenement_participer_form', {id: item.id}) }}" class="btn btn-success btn-lg mb-2">
>>>>>>> isramedi
>>>>>>> 4f714e473f4c6306d8cd13fadaae1828cd26d7f1
                                <i class="bi bi-check-circle me-1"></i> Participer
                            </a>
                            
                            <div class="btn-group btn-group-sm" role="group">
                                <a href="{{ path('app_evenement_show', {id: item.id}) }}" class="btn btn-outline-primary">
                                    <i class="bi bi-eye"></i> Détails
                                </a>
<<<<<<< HEAD
                                {% if is_granted('ROLE_ORGANISATEUR') and item.organisateur and item.organisateur.id == app.user.id %}
=======
<<<<<<< HEAD
                                {% if is_granted('ROLE_ORGANISATEUR') %}
=======
                                {% if is_granted('ROLE_ORGANISATEUR') and item.organisateur and item.organisateur.id == app.user.id %}
>>>>>>> isramedi
>>>>>>> 4f714e473f4c6306d8cd13fadaae1828cd26d7f1
                                    <a href="{{ path('app_evenement_edit', {id: item.id}) }}" class="btn btn-outline-secondary">
                                        <i class="bi bi-pencil"></i> Modifier
                                    </a>
                                    <form method="POST" action="{{ path('app_evenement_delete', {id: item.id}) }}" style="display:inline;">
                                        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ item.id) }}">
<<<<<<< HEAD
                                        <button type="submit" class="btn btn-outline-danger btn-sm" onclick="return confirm('�tes-vous s�r de vouloir supprimer cet �v�nement ?');">
=======
<<<<<<< HEAD
                                        <button type="submit" class="btn btn-outline-danger btn-sm" onclick="return confirm('Êtes-vous sûr de vouloir supprimer cet événement ?');">
=======
                                        <button type="submit" class="btn btn-outline-danger btn-sm" onclick="return confirm('�tes-vous s�r de vouloir supprimer cet �v�nement ?');">
>>>>>>> isramedi
>>>>>>> 4f714e473f4c6306d8cd13fadaae1828cd26d7f1
                                            <i class="bi bi-trash"></i> Supprimer
                                        </button>
                                    </form>
                                {% endif %}
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        {% else %}
            <div class="alert alert-info" role="alert">
                <i class="bi bi-info-circle me-2"></i>
<<<<<<< HEAD
                <strong>Aucun événement publié pour le moment.</strong>
=======
<<<<<<< HEAD
                <strong>Aucun événement pour le moment.</strong>
=======
                <strong>Aucun événement publié pour le moment.</strong>
>>>>>>> isramedi
>>>>>>> 4f714e473f4c6306d8cd13fadaae1828cd26d7f1
                {% if is_granted('ROLE_ORGANISATEUR') %}
                    <a href="{{ path('app_evenement_new') }}" class="alert-link">Créez un nouvel événement</a>
                {% endif %}
            </div>
        {% endfor %}
    </div>
</div>

<!-- Modal for participation -->
<div class="modal fade" id="participateModal" tabindex="-1">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">
                    <i class="bi bi-clipboard-check me-2"></i>Inscrivez-vous à l'événement
                </h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <form id="participateForm" method="post" action="">
                <div class="modal-body">
                    <p class="text-muted mb-4">Veuillez remplir vos informations ci-dessous :</p>
                    
                    <div class="mb-3">
                        <label class="form-label fw-bold">Prénom</label>
                        <input type="text" id="firstName" name="firstName" class="form-control form-control-lg" placeholder="Ex: Jean" required>
                    </div>
                    
                    <div class="mb-3">
                        <label class="form-label fw-bold">Nom</label>
                        <input type="text" id="lastName" name="lastName" class="form-control form-control-lg" placeholder="Ex: Dupont" required>
                    </div>
                    
                    <div class="mb-3">
                        <label class="form-label fw-bold">Email</label>
                        <input type="email" id="email" name="email" class="form-control form-control-lg" placeholder="Ex: jean.dupont@email.com" required>
                    </div>
                    
                    <input type="hidden" id="csrf_token" name="_token" value="">
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">Fermer</button>
                    <button type="submit" class="btn btn-success btn-lg">
                        <i class="bi bi-check me-1"></i>Confirmer l'inscription
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>

{% block scripts %}
<<<<<<< HEAD
=======
<<<<<<< HEAD
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            const participateModal = document.getElementById('participateModal');
            const participateForm = document.getElementById('participateForm');
            
            if (!participateModal || !participateForm) return;

            // When modal is shown, set up the form action and clear fields
            participateModal.addEventListener('show.bs.modal', function (event) {
                const button = event.relatedTarget;
                const eventId = button.getAttribute('data-event-id');

                // Set form action to the participation endpoint
                participateForm.action = "{{ path('app_evenement_participer', {id: '__ID__'}) }}".replace('__ID__', eventId);
                
                // Set CSRF token
                const csrfTokenElement = document.getElementById('csrf_token');
                if (csrfTokenElement && button.hasAttribute('data-csrf')) {
                    csrfTokenElement.value = button.getAttribute('data-csrf');
                }
                
                // Clear form fields
                document.getElementById('firstName').value = '';
                document.getElementById('lastName').value = '';
                document.getElementById('email').value = '';
            });

            // Handle form submission
            participateForm.addEventListener('submit', function (e) {
                e.preventDefault();
                
                const firstName = document.getElementById('firstName').value.trim();
                const lastName = document.getElementById('lastName').value.trim();
                const email = document.getElementById('email').value.trim();

                if (!firstName || !lastName || !email) {
                    alert('Veuillez remplir tous les champs.');
                    return;
                }

                // Submit the form
                participateForm.submit();
            });
        });
    </script>
{% endblock %}

=======
>>>>>>> 4f714e473f4c6306d8cd13fadaae1828cd26d7f1
<script>
document.addEventListener('DOMContentLoaded', function () {
    const participateModal = document.getElementById('participateModal');
    const participateForm = document.getElementById('participateForm');
    
    if (!participateModal || !participateForm) return;

    participateModal.addEventListener('show.bs.modal', function (event) {
        const button = event.relatedTarget;
        const eventId = button.getAttribute('data-event-id');
        participateForm.action = "{{ path('app_evenement_participer', {id: '00000000-0000-0000-0000-000000000000'}) }}".replace('00000000-0000-0000-0000-000000000000', eventId);
        const csrfTokenElement = document.getElementById('csrf_token');
        if (csrfTokenElement && button.hasAttribute('data-csrf')) {
            csrfTokenElement.value = button.getAttribute('data-csrf');
        }
        document.getElementById('firstName').value = '';
        document.getElementById('lastName').value = '';
        document.getElementById('email').value = '';
    });

    participateForm.addEventListener('submit', function (e) {
        e.preventDefault();
        const firstName = document.getElementById('firstName').value.trim();
        const lastName = document.getElementById('lastName').value.trim();
        const email = document.getElementById('email').value.trim();
        if (!firstName || !lastName || !email) {
            alert('Veuillez remplir tous les champs.');
            return;
        }
        participateForm.submit();
    });
});
</script>
{% endblock %}
<<<<<<< HEAD
=======
>>>>>>> isramedi
>>>>>>> 4f714e473f4c6306d8cd13fadaae1828cd26d7f1
{% endblock %}
