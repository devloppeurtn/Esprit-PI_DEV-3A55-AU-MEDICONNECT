{% extends 'admin/base_admin.html.twig' %}

{% block title %}Mes rendez-vous - MediConnect{% endblock %}

{% block body %}
<div class="medecin-rendez-vous">
    <h1 class="h3 mb-4"><i class="bi bi-calendar-event-fill me-2"></i>Mes rendez-vous</h1>

    {% if rdvs|length > 0 %}
    <div class="row g-4">
        {% for r in rdvs %}
        <div class="col-lg-6 col-xl-4">
            <div class="card border-0 shadow-sm h-100 rdv-card">
                <div class="card-body">
                    <div class="d-flex justify-content-between align-items-start mb-3">
                        <div class="d-flex align-items-center gap-2">
                            <div class="rdv-avatar rdv-avatar-patient">
                                <i class="bi bi-person-fill"></i>
                            </div>
                            <div>
                                <h6 class="mb-0 fw-bold">{{ r.patient.nomComplet }}</h6>
                                <small class="text-muted">{{ r.patient.email }}</small>
                            </div>
                        </div>
                        {% if r.statut.value == 'EN_ATTENTE' %}<span class="badge bg-warning text-dark">En attente</span>
                        {% elseif r.statut.value == 'CONFIRME' %}<span class="badge bg-success">Confirmé</span>
                        {% elseif r.statut.value == 'ANNULE' %}<span class="badge bg-danger">Annulé</span>
                        {% else %}<span class="badge bg-secondary">Terminé</span>{% endif %}
                    </div>
                    <div class="mb-3">
                        <i class="bi bi-calendar3 text-primary me-2"></i>
                        <span>{{ r.dateDebut|date('d/m/Y') }}</span>
                        <i class="bi bi-clock ms-3 text-primary me-2"></i>
                        <span>{{ r.dateDebut|date('H:i') }}</span>
                    </div>
                    {% if r.note %}
                    <p class="text-muted small mb-3">{{ r.note|length > 80 ? r.note|slice(0, 80) ~ '...' : r.note }}</p>
                    {% endif %}
                    <div class="d-flex flex-wrap gap-2">
                        <a href="{{ path('app_medecin_dossier_patient', {id: r.patient.id}) }}" class="btn btn-sm btn-outline-info">
                            <i class="bi bi-folder2-open me-1"></i>Dossier médical
                        </a>
                        <a href="{{ path('app_user_profile_view', {id: r.patient.id}) }}" class="btn btn-sm btn-outline-primary">
                            <i class="bi bi-person me-1"></i>Profil patient
                        </a>
                        {% if r.statut.value == 'CONFIRME' and not r.consultation %}
                        <a href="{{ path('app_medecin_consultation_nouvelle', {id: r.id}) }}" class="btn btn-sm btn-primary">Créer consultation</a>
                        {% elseif r.consultation %}
                        <a href="{{ path('app_medecin_consultation_voir', {id: r.consultation.id}) }}" class="btn btn-sm btn-outline-secondary">Voir consultation</a>
                        {% endif %}
                        {% if r.statut.value != 'ANNULE' %}
                        <form method="post" action="{{ path('app_medecin_rdv_annuler', {id: r.id}) }}" class="d-inline" onsubmit="return confirm('Annuler ce rendez-vous ?');">
                            <input type="hidden" name="_token" value="{{ csrf_token('annuler_rdv_' ~ r.id) }}">
                            <button type="submit" class="btn btn-sm btn-outline-danger">Annuler</button>
                        </form>
                        {% endif %}
                    </div>
                </div>
            </div>
        </div>
        {% endfor %}
    </div>
    {% else %}
    <div class="card border-0 shadow-sm">
        <div class="card-body text-center py-5">
            <i class="bi bi-calendar-x display-4 text-muted opacity-50 d-block mb-3"></i>
            <p class="text-muted mb-0">Aucun rendez-vous.</p>
        </div>
    </div>
    {% endif %}
</div>
<style>
.rdv-card { transition: transform 0.2s, box-shadow 0.2s; }
.rdv-card:hover { transform: translateY(-2px); box-shadow: 0 0.5rem 1rem rgba(0,0,0,.1) !important; }
.rdv-avatar { width: 48px; height: 48px; border-radius: 50%; display: flex; align-items: center; justify-content: center; flex-shrink: 0; }
.rdv-avatar-patient { background: rgba(25, 135, 84, 0.15); color: #198754; }
</style>
{% endblock %}
