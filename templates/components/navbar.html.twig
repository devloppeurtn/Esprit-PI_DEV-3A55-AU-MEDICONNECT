{#
    Navbar MediConnect - Utilisée sur toutes les pages (home, auth, admin)
    Variables optionnelles:
    - is_admin_context: true pour les interfaces admin (affiche le toggle sidebar)
#}
{% set is_admin = is_admin_context|default(false) %}
{% set dashboard_home_route = 'app_home' %}
{% if is_admin and app.user %}
    {% if is_granted('ROLE_ADMIN') %}
        {% set dashboard_home_route = 'app_admin_dashboard' %}
    {% elseif is_granted('ROLE_MEDECIN') %}
        {% set dashboard_home_route = 'app_medecin_index' %}
    {% elseif is_granted('ROLE_SECRETAIRE') %}
        {% set dashboard_home_route = 'app_secretaire_index' %}
    {% elseif is_granted('ROLE_PATIENT') %}
        {% set dashboard_home_route = 'app_patient_index' %}
    {% endif %}
{% endif %}

<header id="header" class="header fixed-top {{ is_admin ? 'navbar-admin-context' : '' }}">
    <div class="topbar d-flex align-items-center dark-background">
        <div class="container d-flex justify-content-center justify-content-md-between">
            <div class="contact-info d-flex align-items-center">
                <i class="bi bi-envelope d-flex align-items-center"><a href="mailto:contact@mediconnect.fr">contact@mediconnect.fr</a></i>
                <i class="bi bi-phone d-flex align-items-center ms-4"><span>+216 52 251 112</span></i>
            </div>
            <div class="social-links d-none d-md-flex align-items-center">
                <a href="#!" class="twitter"><i class="bi bi-twitter-x"></i></a>
                <a href="#!" class="facebook"><i class="bi bi-facebook"></i></a>
                <a href="#!" class="instagram"><i class="bi bi-instagram"></i></a>
                <a href="#!" class="linkedin"><i class="bi bi-linkedin"></i></a>
            </div>
        </div>
    </div>

    <div class="branding d-flex align-items-center {{ is_admin ? 'branding-admin' : '' }}">
        <div class="container position-relative d-flex align-items-center justify-content-between">
            {% if is_admin %}
                <button class="navbar-sidebar-toggle" type="button" data-admin-sidebar-toggle aria-label="Menu">
                    <i class="bi bi-list"></i>
                </button>
            {% endif %}

            <a href="{{ path(dashboard_home_route) }}" class="logo d-flex align-items-center">
                <img src="{{ asset('assets/img/logomed.png') }}" alt="Logo MediConnect" {{ is_admin ? 'height="32"' : '' }}>
                <h1 class="sitename">MediConnect</h1>
            </a>

            <nav id="navmenu" class="navmenu">
                <ul>
                    <li><a href="{{ path('app_home') }}" {% if app.request.get('_route') == 'app_home' %}class="active"{% endif %}>Accueil</a></li>
                    <li><a href="{{ path('app_catalogue') }}" {% if app.request.get('_route') starts with 'app_catalogue' %}class="active"{% endif %}>Catalogue</a></li>
                    <li><a href="{{ path('app_evenement_index') }}" {% if app.request.get('_route') starts with 'app_evenement_' %}class="active"{% endif %}>
                        <i class="bi bi-calendar-event me-1"></i> &Eacute;v&eacute;nements
                    </a></li>
                    <li><a href="{{ path('app_home') }}#home-about">&Agrave; propos</a></li>
                    <li><a href="{{ path('app_home') }}#featured-services">Services</a></li>
                    <li><a href="{{ path('app_home') }}#footer">Contact</a></li>
                    {% if app.user %}
                        {# Savoir Médical : accessible à tous les utilisateurs connectés #}
                        <li><a href="{{ path('app_savoir_medical_index') }}" {% if app.request.get('_route') starts with 'app_savoir_medical_' %}class="active"{% endif %}><i class="bi bi-book me-1"></i>Savoir Médical</a></li>
                        <li><a href="{{ path('app_orders_list') }}" {% if app.request.get('_route') starts with 'app_orders_' %}class="active"{% endif %}><i class="bi bi-bag me-1"></i>Mes commandes</a></li>
                        {# Admin : lien vers tableau de bord #}
                        {% if is_granted('ROLE_ADMIN') %}
                        <li><a href="{{ path('app_admin_dashboard') }}" {% if app.request.get('_route') starts with 'app_admin_' %}class="active"{% endif %}><i class="bi bi-speedometer2 me-1"></i>Administration</a></li>
                        {% endif %}
                        {# Médecin : lien espace médecin (si route existe) #}
                        {% if is_granted('ROLE_MEDECIN') %}
                        <li><a href="{{ path('app_medecin_index') }}" {% if app.request.get('_route') starts with 'app_medecin_' %}class="active"{% endif %}>Espace Medecin</a></li>
                        {% endif %}
                        {% if is_granted('ROLE_PATIENT') %}
                            <li class="nav-item me-2 d-flex align-items-center">
                                <a href="{{ path('app_patient_notifications') }}" class="position-relative text-dark fs-5" title="Notifications">
                                    <i class="bi bi-bell"></i>
                                    {% set unread_count = app.user.unreadNotificationsCount %}
                                    {% if unread_count > 0 %}
                                        <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger" style="font-size: 0.6rem;">
                                            {{ unread_count > 9 ? '9+' : unread_count }}
                                        </span>
                                    {% endif %}
                                </a>
                            </li>
                        {% endif %}
                        {# Menu utilisateur style Bootstrap (Profile, Paramètres, Déconnexion) #}
                        <li class="nav-user-dropdown">
                            <div class="dropdown">
                                <button class="btn btn-user-dropdown d-flex align-items-center" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                                    <span class="user-avatar-profile me-2"><i class="bi bi-person-circle"></i></span>
                                    <span class="user-name-text d-none d-md-inline">{{ app.user.nomComplet }}</span>
                                    <i class="bi bi-chevron-down ms-1 dropdown-chevron"></i>
                                </button>
                                <ul class="dropdown-menu dropdown-menu-end">
                                    <li><a class="dropdown-item" href="{{ path('app_profile') }}"><i class="bi bi-person me-2"></i>Profil</a></li>
                                    <li><a class="dropdown-item" href="{{ path('app_settings') }}"><i class="bi bi-gear me-2"></i>Paramètres</a></li>
                                    <li><hr class="dropdown-divider"></li>
                                    <li><a class="dropdown-item" href="{{ path('app_logout') }}"><i class="bi bi-box-arrow-right me-2"></i>Déconnexion</a></li>
                                </ul>
                            </div>
                        </li>
                    {% else %}
                        <li><a href="{{ path('app_login') }}" {% if app.request.get('_route') == 'app_login' %}class="active"{% endif %}>Connexion</a></li>
                        <li><a href="{{ path('app_signup') }}" class="btn-nav-signup {% if app.request.get('_route') == 'app_signup' %}active{% endif %}">S'inscrire</a></li>
                    {% endif %}
                </ul>
                <i class="mobile-nav-toggle d-xl-none bi bi-list"></i>
            </nav>
        </div>
    </div>
</header>
