{% extends 'base.html.twig' %}
{% block title %}Paiement reçu - MediConnect{% endblock %}
{% block body %}
<div class="container py-5">
    <div class="row">
        <div class="col-md-8 offset-md-2">
            <div class="alert alert-success">
                <h4 class="alert-heading">Paiement reçu</h4>
                <p>Merci ! Votre paiement pour la commande #{{ commande.id }} a été reçu.</p>
                <hr>
                <a href="{{ path('app_orders_list') }}" class="btn btn-primary">Voir mes commandes</a>
                <a href="{{ path('app_order_detail', {id: commande.id}) }}" class="btn btn-outline-primary">Détail de la commande</a>
            </div>
        </div>
    </div>
</div>
{% endblock %}
