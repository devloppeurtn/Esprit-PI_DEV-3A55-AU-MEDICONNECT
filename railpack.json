{
  "packages": {
    "php": "8.2"
  },
  "deploy": {
    "startCommand": "composer install --no-dev --optimize-autoloader --no-interaction && APP_ENV=prod php bin/console cache:clear && php -S 0.0.0.0:$PORT -t public"
  }
}
